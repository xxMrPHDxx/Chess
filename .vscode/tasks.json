{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Make & Run",
            "type": "shell",
            "command": "cmake -G \"MinGW Makefiles\" . && make && chess",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}